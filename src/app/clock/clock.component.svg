<!--  style="" -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" [ngStyle]="{width: size, height: size}" (click)="click();">
  <path [attr.d]="part.path" *ngFor="let part of parts" [attr.fill]="part.fill" stroke="black"/>
</svg>
